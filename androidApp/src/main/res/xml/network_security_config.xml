<?xml version="1.0" encoding="utf-8"?>
<!--
    Android has blocked cleartext HTTP by default since API 28, and the local
    dz-server runs over plain HTTP.

    The exception is limited to the loopback addresses a developer machine is
    reached on, so it is safe to ship: those hosts are unreachable in
    production, and every other domain still requires HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- The emulator's alias for the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
